"use client";
import { styled } from "@mui/material/styles";
import Typography from "@mui/material/Typography";

export const _Fullname = styled(Typography)(() => ({
  fontSize: "16px",
  fontWeight: 700,
}));

export const _Subinfo = styled(Typography)(() => ({
  fontSize: "12px",
  fontWeight: 400,
  color: "gray"
}));
